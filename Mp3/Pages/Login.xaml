<Page
    x:Class="Mp3.Pages.Login"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Mp3.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center">
            <TextBlock HorizontalAlignment="Center" FontSize="24">Login</TextBlock>
            <ScrollViewer Margin="20">
                <StackPanel Margin="50" HorizontalAlignment="Left" VerticalAlignment="Top">

                    <TextBlock>Email</TextBlock>
                    <TextBox Width="200" x:Name="email"></TextBox>

                    <TextBlock>Password</TextBlock>
                    <PasswordBox Width="200" x:Name="password"></PasswordBox>

                    <Button Click="ButtonLogin_Click">Login</Button>
                    <Button Click="ButtonReset_Click">Reset</Button>
                </StackPanel>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
